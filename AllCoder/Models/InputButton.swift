class InputButton: Decodable {
    
    let id: Int
    let index: Int
    let startIndex: Int
    let endIndex: Int
    let lineNumber: Int
    let createdAt: String
    let updatedAt: String
    
}
